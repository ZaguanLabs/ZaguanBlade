//! Integration configuration and identities. Protocol runtimes attach separately;
//! saving configuration never starts a process or grants launch permission.

pub mod config;
pub mod identity;
pub mod store;
